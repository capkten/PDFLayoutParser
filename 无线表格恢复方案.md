1. 当前问题：为什么需要重新设计表格恢复流程

本方案面向可解析的原生财报 PDF。前期对港股、A 股和 SEC 财报样本的表格区域检测、无线表格单元格恢复和二维矩阵实验表明：表格区域检测、原始单元格恢复、行列骨架恢复和合并单元格推断是四个不同问题，不能使用一套简单规则一次完成。

1.1 当前方法暴露的主要问题
问题层级	典型现象	根本原因	设计要求
表格区域	目录、列表、多栏正文被误检；无线表漏检；检测框偏大或偏小	线条规则只擅长有线表，文本对齐又容易把非表格结构当表格	区域检测与结构恢复分离；使用 YOLOv8-OBB 检测表格区域，再映射回原生 PDF 对象
原始单元格	同一文本条被拆碎；换行项目、角标、币种金额单位和中英文层无法正确归并	PDF Span 是排版对象，不等于逻辑单元格；原始输出顺序只是证据之一	先恢复单语文本条，再建立列保护，最后进行中英文逻辑单元格配对
行列结构	左侧缩进被切成多列；金额列漂移；虚拟列线穿过合并表头	把所有 x 起点都当列，或用单行最大 token 数决定列数	按列角色恢复：项目列、双语列、货币符号列、金额列分别建轨迹
合并单元格	应合并未合并、不应合并被合并、局部表头跨度错误	只根据空白或 bbox 高度猜 rowspan/colspan	使用 bbox 覆盖、稳定轨迹、完整数据组和占位冲突共同判断
工程迭代	修复一个样本后其他样本退化，难以判断错误发生在哪一层	提取、聚类、行列和合并规则混在同一循环中	模块化处理、逐层快照、固定错误样本回归和整表完全正确率
1.2 已验证失败的简化方案
失败方案	为什么失败	当前修复方向
Span/word 直接等于单元格	PDF 排版对象不等于逻辑单元格，多行表头和换行项目被拆碎	同行近邻 + 同列连续 Span 链
连续输出直接跨行合并	项目列本身连续输出，相邻项目会被吞成大框	增加同列、行距、样式和其他列对象打断条件
$ 与金额普通拼接	固定距离无法判断财务列归属	$ 独立轨迹；金额按右边界聚类
左侧所有 x 起点都建立列	项目层级缩进被误当二维列	项目列 + indent_level；双语列必须有多行重复证据
最大 token 数决定列数	$、缺值、破折号和合并表头都会改变 token 数	稳定重复轨迹 + 密集行补全 + 列角色
全局虚拟边界贯穿表格	把对齐轨迹误当实体边界，切过合并表头和宽文本	边界分段；遇文字或合并格即抑制
根据空白猜合并单元格	空白可能只是缺值，不是合并的正向证据	bbox、轨迹、完整数据组和占位冲突共同约束
1.3 典型失败案例与修复结果

下面九类案例不是孤立样本，而是此前迭代中反复出现、可以稳定复现的错误机制。每类都给出失败方案、失败原因、修复前后结果和当前修复规则。

案例一：同一个逻辑文本条被拆散

问题：原生 PDF 会把一个逻辑文本条拆为多个 Span：普通词片段、年份与单位、币种与金额、上标/脚注、同列换行都可能分别输出。为修复跨列误合并而过早应用列边界，又会把本应合并的 人民币 + 10,000,000 + 元等内容重新拆开。

两个代表性例子：

表头中的 As at 31 December / 於12月31日、各年度、HK$'000 / 千港元，每一列应恢复为完整的单语文本条，角标附着在所属文本条上。
附属公司表中的多行公司名称、人民币 + 金额 + 元、Corporate management / service和Non-performing asset / investment，应先在各自单语列内恢复完整。
例子A：原始 Span 被拆散	例子A：文本条恢复后



例子B：列保护过早导致合法内容仍被拆分	例子B：v8恢复文本条并避免跨列误合并


合并时必须同时考虑：① 同一原生 block/line及连续 span_index；② 水平词间距与垂直重叠；③ 上标、下标和脚注与正文的附着关系——同原生行、紧贴正文的角标即使 bbox 轻微横向重叠也应吸附；④ 原始输出连续性、同列 x 重叠和合理行距；⑤ 小写续词或有明确缩进的孤立续行；⑥ 续行判断按列进行，其他列恰好在同一视觉行换行不能打断本列；⑦ 稳定列轨道只用于阻止普通文本跨列，不得打断已确认的原生文本行；⑧ 独立 $不参与普通文本条拼接。

修复规则：先执行“单语文本条恢复”——原生行内重组、角标吸附、同列换行续接；再使用列轨道防止普通同行跨列。这样既不会把公司名称与 Hong Kong合并，也不会因右侧业务描述并行换行而拆散左侧公司名称，更不会拆散币种、金额、单位和换行描述。

案例二：Span 排版对象被直接当作逻辑单元格

问题：多行表头、日期、年份和单位被拆成多个碎片，单纯按 y/x 重排还会丢失原始内容流。

错误结果：表头与日期被拆成多个 Span	修复结果：原始顺序 + 同列约束恢复 Span 链


修复规则：先做同行近邻拼接，再对原始顺序连续、同列且行距连续的 Span 建链。

案例三：连续输出导致多个独立项目被误合并

问题：财报项目列本身就是连续输出的，连续顺序并不能证明属于同一个单元格。

错误结果：多个独立项目被吞成大框	修复结果：保守 Span 链保持逐行对应


修复规则：跨行建链必须同时满足同列 x 区间、连续行距、样式相容，并且没有其他列对象打断；普通项目默认不产生 rowspan。

案例四：货币符号与金额被误合并

失败方案：同一行中只要距离较近就拼接，没有给货币符号建立独立列角色。

失败原因：$ 可能作为独立 Span 输出，也可能尾随在数字 Span 中；不同金额列的间距可能小于长表头的词间距，固定距离阈值无法判断归属。

错误结果：$ 与相邻数值形成错误组合	修复结果：$ 与金额保持独立轨迹


修复规则：$ 禁止参与普通文本拼接；尾随但视觉独立的货币符号先拆分；货币符号按中心点建列，金额按右边界建列。

案例五：左侧缩进形成伪列并干扰金额列

问题：把左侧所有 x 起点都建立成列，会将“其中：”、子项目和换行续写的层级缩进误当作二维列，并进一步干扰金额列和合并关系。

错误结果：左侧项目区被切成大量伪列	修复结果：项目区、货币符号与金额轨迹分离


修复规则：左侧默认一个项目列，缩进只保存为 indent_level；货币符号按中心点、金额按右边界分别建立稳定轨迹。

案例六：用最大单元格数量直接决定列数

失败方案：候选单元格最多的一行决定全表列数，其他行再按中心点映射。

失败原因：货币符号、空值、破折号、括号负数和合并表头都会改变 token 数量；金额宽度不同还会导致中心点漂移。

错误结果：多个独立数据列被压入同一列	修复结果：按财务列角色恢复稳定列轨迹


修复规则：金额按右边界、货币符号按中心点聚类；多行重复轨迹提供稳定性，最密集数据行只用于补全，不能直接决定列数。

案例七：全局虚拟网格贯穿并切割内容

失败方案：把每条虚拟列边界从表格顶部一直画到底部。

失败原因：无线表格的列是对齐轨迹，不是实体边框；合并表头、局部表头和中间重复表头会跨越多个轨迹。

错误结果：虚拟边界切过表头或错误暗示分列	修复结果：分段边界与局部合并表头


修复规则：列边界逐行绘制，行边界逐列绘制；当前段存在 colspan、rowspan 或文本 bbox 穿越时抑制边界。

案例八：根据空白位置猜测合并单元格

失败方案：某行比最大列数少就向空列扩展；下方同列为空就自动生成 rowspan。

失败原因：空白可能只是缺值，普通换行项目 bbox 也可能较高；缺少内容不是合并单元格的正向证据。

错误结果：合并范围被伪列和空白驱动	修复结果：bbox 与稳定轨迹共同驱动合并


修复规则：仅表头候选使用 bbox 推断跨度；触碰“货币符号 + 金额”组合时扩到完整组；覆盖已有独立格时拒绝扩展并报告冲突。

案例九：中英文内容没有恢复为同一个逻辑单元格

问题：双语财报经常将英文、中文分别输出为两个文本层。英文公司名称或业务描述可能先换行，导致其他列与中文层之间留出一行空白；若沿用普通文本换行的短距离阈值，会漏掉应配对的中英文内容。

原始 Span：中英文、单位和数值各自独立	修复结果：中英文文本条形成双语逻辑单元格


配对条件：先完成两侧的单语文本条恢复；再要求同一列轨道或明显 x 重叠、语言互补、表内存在稳定双语节奏、两者之间没有另一条同列文本。配对窗口允许英文换行带来的额外留白，但不能无限放宽。

边界：Male / 男性、hours/employee / 小时／僱員、公司名称、地点、金额单位和业务描述可以组成双语逻辑单元格；右侧 30、12、100%跨中英文两行的关系属于后续 rowspan推断，不在此阶段把数值强行与文本合并。

1.4 本轮设计结论
不使用通用版面分析模型，改为“页面区域构建”。
页眉页脚使用跨页统计和位置规则；图片使用原生位图对象和矢量绘图聚类；表格使用 YOLOv8-OBB 检测。
表格区域确定后，再做结构恢复，区域检测模型不直接生成行列和单元格。
混合表格不再设置独立恢复器：先进入有线恢复，失败或低置信再整体回退无线恢复。
模板匹配位于有线、无线通用算法之后，人工复核之前，只解决稳定版式的第三兜底场景。
2. 无线表格结构恢复方案
2.1 适用前提与第一原则

适用于表格区域已经确认的原生 PDF。第一步不是直接画全局行列网格，而是读取 page.get_text("dict", sort=False)，保留原始 Span 输出顺序，再结合坐标、间距、字体、样式和同列关系恢复原始候选单元格。

原始输出顺序是重要证据，但不是充分条件。任何拼接和跨行合并都必须同时满足空间关系与稳定列轨迹约束。

2.2 模块化恢复流程 M1～M9
M1 原生 Span 提取：保留 order、source_position、bbox、字体、字号、粗体和 span_ref。
M2 Span 规范化：清理空对象；处理上标；对数字尾随但视觉独立的货币符号进行保守拆分。
M3 原始候选单元格：分为两个子模块：M3A 单语文本条恢复（原生行内重组、角标吸附、同列换行续接、列边界防误合并）；M3B 中英文逻辑单元格配对（同列、语言互补、双语节奏和宽间距留白约束）。
M4 视觉行：按候选单元格 y 中心聚类；先保留全部视觉行，不提前折叠项目层级。
M5 列角色：项目列、双语项目列、货币符号列和金额列分别恢复。
M6 单元格映射：项目、金额、货币符号和局部表头只在对应角色中寻找轨迹。
M7 合并单元格：根据 bbox 覆盖的稳定行列轨迹推断 rowspan/colspan。
M8 网格校验：检查重复占位、越界、角色冲突和合并格覆盖独立单元格。
M9 二维矩阵：生成 cell、covered、empty 三类位置。
2.3 原始候选单元格判断
判断	必须满足	立即终止
M3A 原生行内重组	同一 block/line；span_index 连续；间距小且垂直相交；角标与正文邻近	独立 $；明显跨列空白走廊；无垂直相交
M3A 同列换行续接	原始输出连续；x 区间重叠；垂直行距连续；小写续词或有明确缩进的孤立续行	下一 Span 属于其他数据列；无缩进的独立项目；行距过大；层级变化
M3B 中英文逻辑配对	两侧单语文本条已完成；同列/重叠；语言互补；稳定双语节奏；无同列内容插入	语言相同；跨列；超出双语行带；存在待配对的同列文本
2.4 行列与列角色恢复
左侧项目区默认是一列；缩进保存为 indent_level，不生成伪列。
只有多行稳定同时出现两块独立文本时，才建立双语项目列。
金额按右边界 x1 聚类，避免不同宽度金额中心漂移。
货币符号按中心点建立独立列角色，禁止与普通文本直接拼接。
多行重复轨迹提供稳定性；数据最密集行只用于补全，不能直接决定列数。
项目区和数据区边界放在真实空白走廊中。
局部表头可以出现在表格任意位置，不限定于最顶部。
2.5 合并单元格恢复
表头 bbox 覆盖的稳定列轨迹确定 colspan，覆盖的行轨迹确定 rowspan。
触碰“货币符号 + 金额”组合的一部分时，按完整数据组扩展。
普通项目和金额默认保持 1×1；换行项目不因 bbox 较高自动生成 rowspan。
空白不是合并的正向证据。
扩展后覆盖已有独立单元格时，拒绝扩展并记录冲突。
最终矩阵中的任何重复占位都必须报告，不能静默覆盖。
2.6 无线恢复器输入与输出

输入：table bbox、区域内原生 Span、字体样式、对象顺序、可靠局部线段及来源引用。

输出：候选单元格、视觉行、列角色、行列轨迹、rowspan/colspan、二维矩阵、confidence、evidence 和 issues。

3. PDF 解析整体方案设计

整体采用“对象提取优先、模型职责单一、模块可替换、过程可诊断”的设计。表格区域检测与表格结构恢复分离；有线恢复、无线回退和模板兜底最终汇入统一 Table IR。

3.1 完整解析算法流程

3.2 页面区域构建
区域	主要证据	输出
页眉 / 页脚	顶部/底部位置带、跨页重复文本与对象指纹、页码和字体样式一致性	header / footer region
表格	YOLOv8-OBB 在渲染页上检测，随后映射回 PDF 坐标并裁取原生对象	table polygon、confidence、object refs
图片 / 图形	image block/xref 原生 bbox；vector drawing 空间聚类；邻近标题和图注	figure / raster_image / vector_chart

三路结果经过区域冲突消解，形成 Page Region IR：header、footer、table、figure 和 residual_body。表格内图片保留为单元格对象；整页位图另标为 scanned/background。

3.3 表格恢复协调器
提取表格区域内 Span、线条、图形和 bbox，同时只生成模板指纹候选。
线证据充分时进入有线恢复；混合表格也优先进入此分支，文本仅补全局部缺失边界。
有线结果校验失败或低置信时，整体回退无线恢复。
无线结果校验失败后，才允许模板匹配接管。
模板匹配仍失败时保留全部候选对象和问题清单，进入人工复核。
3.4 主要功能模块
模块	输入	主要职责	输出
M0 文档与页面输入	PDF 文件	文档元数据、页树、页面尺寸和旋转信息	Document/Page Context
M1 原生对象提取	Page	提取 Span、字体、原始顺序、嵌入图片、矢量绘图和来源引用	Page Object IR
M2 坐标与对象规范化	Page Object IR	坐标互转、旋转裁剪、对象 ID 和可追溯引用	Normalized Object IR
M3 页面区域构建	原生对象 + 渲染页	页眉页脚规则、YOLOv8-OBB 表格检测、图片与矢量图形聚类、冲突消解	Page Region IR
M4 表格恢复协调器	table region + 原生对象	有线优先、无线回退、模板第三兜底	候选 Table IR
M5 统一结构校验	候选 Table IR	文本覆盖、占位冲突、行列稳定性、合并关系和财务一致性	ValidationIssue[]
M6 二维矩阵	已校验 Table IR	明确区分主单元格、合并覆盖位置和结构空位	cell / covered / empty
M7 置信度与人工复核	结构与证据	高置信输出；失败保留对象、证据和问题清单	Final Table + confidence
M8 统一导出	页面结构、二维表格	输出 JSON、HTML、Excel 和页面阅读顺序	解析结果
4. 统一 Table IR 与质量门

三条恢复路径统一输出 bbox、rows、columns、cells、row_start/row_end、col_start/col_end、rowspan/colspan、source_span_refs、evidence、confidence 和 issues。

cell：主单元格；
covered：被合并单元格覆盖，并指向 master_cell_id；
empty：结构性空位。

统一质量门检查文本映射覆盖率、重复占位、越界、缺列、列角色冲突、合并范围冲突、金额与年份列合理性，以及可选的合计和财务勾稽关系。

5. 工程化闭环与评估
每层保存中间 JSON、可追溯对象引用和诊断快照。
每层生成原图、错误结果和修复结果可视化。
港股、A 股、SEC 分层抽样，建立区域、单元格、行列、合并格四级金标。
固定错误样本回归，禁止只验证当前修改样本。
同时统计模块指标与整表完全正确率。
新版本输出到新目录，保留历史诊断结果。
6. 当前状态与下一步

当前：十个典型无线财报表格已生成二维矩阵，当前矩阵内部占位冲突为 0；模块化数据结构、原始单元格恢复顺序和基本行列角色方案已经验证可行，但仍需要扩大金标后统计整表准确率。

扩大无线表格单元格、行列和合并格金标；
将当前研究脚本整理为正式模块接口；
实现有线表格恢复器及其失败回退条件；
接入 YOLOv8-OBB 表格区域检测与 PDF 坐标映射；
建立模板库版本、适用范围和匹配后校验；
建立模块级准确率和整表完全正确率。
