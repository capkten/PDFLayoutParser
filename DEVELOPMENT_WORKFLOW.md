# PDFLayoutParser 开发与调试规范

本文档记录本项目中反复使用的调试、验证和交付约定，后续处理 PDF 表格问题时默认遵循。

## 1. 代码定位顺序

仓库存在 `.codegraph/` 时，先使用 CodeGraph 理解调用链和定位符号，再使用 `rg` 查找补充细节：

```powershell
codegraph explore "TableExtractor WirelessTableExtractor extract_zebra"
```

需要查看当前源码时，优先查询具体类名、方法名或调用关系。不要在尚未理解入口和数据流时直接修改代码。

## 2. 系统化调试

遇到错误、测试失败或结果异常时，先使用系统化调试流程：

1. 明确复现步骤和输入文件。
2. 检查当前分支、最近提交和工作区改动。
3. 沿调用链追踪输入、候选区域、提取器和最终输出。
4. 对比正常页面与异常页面的线条、文字、语言、bbox 和表格来源。
5. 先提出一个可验证的根因假设，再做最小修改。
6. 修复前先增加能复现问题的回归测试。

不根据截图或直觉直接猜测修复点；如果连续多次修复仍失败，应重新检查整体架构和数据流。

## 3. 单页 PDF 调试

默认使用项目根目录的 `test_single.py` 做单页解析和可视化。脚本中的配置包括：

- `TARGET_PDF_PATH`：待检查 PDF。
- `OUTPUT_DIR`：输出目录。
- `MODEL_PATH`：表格检测模型。
- `run_single_test()`：默认解析第 0 页。

运行默认第一页：

```powershell
python test_single.py
```

脚本参数是 PDF 路径，不是页码；如果要检查指定页，优先使用小型 Python 调试片段调用 `PDFParser.parse(page_indices=[页索引])`，其中 PDF 页码索引从 0 开始。输出目录应使用带页码和用途的独立目录，例如：

```text
output/single_page_page_098_language/
output/zh_all_table_pages_rerun_language/
```

不要把新结果和旧结果混在同一目录中。

## 4. 全量 PDF 检查

全量重跑时使用 `PDFParser` 并开启可视化输出；长任务使用可持续终端会话，不能以 30 秒等待超时作为任务完成依据。完成后检查：

- 输入 PDF 总页数。
- `pages/` 是否覆盖最后一页。
- `tables/` 中可视化 PNG 数量和最后文件名。
- 有表格页数量和总表格数量。
- 关键异常页的结构化结果与可视化结果。

如果任务中断，必须记录已完成范围，继续处理剩余页后再汇报全量完成。

## 5. 视觉检查

涉及表格区域、bbox、行列结构或可视化 PNG 时，必须同时检查结构化输出和图片。重点确认：

- 表格框没有吸收表格外标题、页眉或正文。
- 多个相邻表格没有被合并。
- 表格内部没有被拆成多个错误区域。
- 行列数、单元格边界和文字归属合理。
- 新输出不是旧目录中的残留文件。

## 6. 表格提取约定

- 有线表格：只保留属于同一连通组件的横线和竖线；横竖线需要实际相交或接触。
- 无线表格：先按页面语言分流。
- `en` 页面可以尝试英文斑马纹颜色背景提取，失败后再走文本对齐。
- `zh` 和 `mixed` 页面跳过 `extract_zebra()`，直接走文本对齐逻辑。
- 中文/混合页面不能仅因存在整页白色背景矩形而进入斑马纹提取。
- ML 检测到区域后，仍需优先复用重叠的有线表格结果；没有有线结果时才按语言选择无线逻辑。

## 7. 测试与提交

实现修复时遵循测试先行：

1. 添加最小失败测试并确认它确实失败。
2. 做最小生产代码修改。
3. 运行新增测试、相关模块测试和页面级验证。
4. 运行 `git diff --check`。
5. 更新 `changes.md`，说明日期、问题、修复和验证结果。
6. 只提交本次任务相关文件，不覆盖或清理用户已有工作区改动。

相关测试命令示例：

```powershell
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD='1'
& 'C:\Users\23662\AppData\Local\Programs\Python\Python312\Scripts\pytest.exe' -q tests/test_wired_table_extractor.py
```

如果完整测试受到缺失 PDF、模型路径或已有环境问题影响，必须明确列出失败数量和原因，不能表述为“全部通过”。

## 8. 结果汇报

每次页面级验证至少汇报：

- 输入 PDF 和页索引/页码。
- 语言分类结果。
- 表格数量、source、行列数和 bbox。
- 可视化结果的绝对路径。
- 测试命令及通过/失败情况。
- 未提交的无关工作区改动是否保留。
